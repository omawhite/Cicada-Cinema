# Brand fonts

Web-licensed `.woff2`/`.woff` files for Reuben and Florensans, obtained
directly from the foundries under a webfont license. See `@font-face`
declarations in `src/global.css`.

Do not commit desktop `.otf`/`.ttf` files or the Illustrator/EPS logo
masters — those live in shared storage, not the repo.
